import styles from './select.module.scss';

export const Select = () => {
  return( 
    <div className={styles.select}>
      <select>
        <option value=""></option>
      </select>
    </div>
  )
}